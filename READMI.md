# BookManager

Универсальная система управления библиотечными ресурсами, построенная на архитектуре **Django REST Framework (DRF)** в бэкенде и **Vue.js** в фронтенде. Проект позволяет управлять книгами, читателями, выдачей книг и гибко настраивать локации.

## 🛠 Технологии

* **Бэкенд**: Python 3.11+, Django 4.2+, Django REST Framework
* **Фронтенд**: Vue 3 (Composition API), Vite, Pinia, Vue Router
* **База данных**: PostgreSQL (рекомендуется) или SQLite (для разработки)

## 📁 Структура проекта

```
bookmanager/
├── backend/
│   └── bookmanager/
│       ├── books/        # Управление книгами (название, автор, ISBN)
│       ├── loans/        # Выдача и возврат книг
│       ├── readers/      # Управление читателями
│       ├── locations/    # Универсальное приложение локаций
│       ├── bookmanager/  # Настройки Django: settings.py, urls.py
│       └── manage.py
├── frontend/
│   └── vue-project/
│       └── src/          # Основной код Vue.js
│           ├── views/    # Страницы приложения
│           ├── components/ # Переиспользуемые компоненты
│           ├── router/   # Маршрутизация
│           ├── layouts/   # 
├── .env.example          # Переменные окружения
└── README.md
```

## 🌍 Приложение `locations` — универсальное решение

Приложение `locations` разработано как **многоразовый модуль**, который можно использовать не только для библиотек, но и в других системах — например, складах, университетах или сетях магазинов.

### Возможности

* Иерархия локаций: `Локация → Стеллаж → Полка`
* Возможность привязки любой сущности (книга, оборудование, сотрудник) к локации

## 🚀 Запуск проекта

<!-- ### Через Docker (рекомендуется)

```bash
cp .env.example .env
docker-compose up --build
``` -->

Сервисы:

* **Бэкенд**: [http://localhost:8000/](http://localhost:8000/)
* **Фронтенд**: [http://localhost:5173/](http://localhost:5173/)

### Локально 

**Бэкенд**

```bash
cd backend/bookmanager/
python -m venv venv
source venv/bin/activate  # Linux/Mac
# или venv\Scripts\activate (Windows)
pip install -r requirements.txt
python manage.py migrate
python manage.py runserver
```

**Фронтенд**

```bash
cd frontend/vue-project/
npm install
npm run dev
```

<!-- ## 🔐 Аутентификация

* Регистрация и вход через JWT
* Роли: администратор, библиотекарь, читатель
* Защита API с помощью `IsAuthenticated` и `DjangoModelPermissions` -->

## 📚 Примеры API-запросов

**Получить список книг**

```http
GET /book/
    {
        "id": 36,
        "title": "Красная Шапочка",
        "author": "Евгений Шварц",
        "year": 2016,
        "genre": "Drama",
        "isbn": "9785446705245",
        "pages": 80,
        "description": "Файл электронной книги подготовлен в Агентстве ФТМ, Лтд., 2013.",
        "cover": "http://books.google.com/books/content?id="****",
        "total_copies": 5,
        "available_copies": 5,
        "location_root": 2,
        "rack": 4,
        "section": null,
        "location_root_name": "Центральная Библиотека",
        "rack_number": "Стеллаж 1"
    }
```

**Выдать книгу читателю**

```http
GET /loan/
    {
        "id": 17,
        "loan_date": "23.11.2025",
        "due_date": "07.12.2025",
        "return_date": "",
        "book": 19,
        "reader": 3
    },
```

**Список библиотек**

```http
GET /locations/roots/
[
    {
        "id": 1,
        "name": "Публичная Библиотека",
        "description": "ул. Народная д.1"
    },
    {
        "id": 2,
        "name": "Центральная Библиотека",
        "description": "ул. Центральная д.1"
    }
]
```

## 🧪 Тестирование

```bash
# Запуск тестов DRF
python manage.py test 

```

## 🔍 Автозаполнение книг

Проект поддерживает автозаполнение информации о книгах через **Google Books API**, реализованное на бэкенде:

- Запрос выполняется на эндпоинт:

http://127.0.0.1:8000/book/autocomplete/?title=<название книги>


- Параметр `title` — часть или полное название книги.  
- Система возвращает список книг с информацией: название, автор, ISBN, год издания, обложка и описание.  
- Это позволяет быстро находить книги и добавлять их в базу без ручного ввода всех данных.

Пример запроса:

```http
GET /book/autocomplete/?title=Идиот
Пример ответа:
{
  "title": "Идиот",
  "author": "Фёдор Достоевский",
  "year": 1869,
  "isbn": "978-5-17-123456-7",
  "cover": "http://books.google.com/...",
  "description": "Краткое описание книги..."
}
```


## 🤝 Автор

Разработано с ❤️ для эффективного управления библиотечными ресурсами.
Для вопросов и предложений: [пишите](nagulin03@mail.ru)
